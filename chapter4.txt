SELECT文・・・DBの問い合わせを行う際に使用する
ex)
SELECT カラム名1, カラム名2, ・・・ FROM 対象のテーブル (WHERE 条件);

・カラム名に*を使うと、対象テーブルのカラムを全て指定する


AS句・・・カラムに別名を付ける
DISTINCT・・・重複行を削除する
WHERE・・・取得したいレコードの条件を指定する

ANDやORなどの論理演算子を使用する際、()を使うと優先度が上になる
ORDER BY句・・・並べかえを行う。ASCは昇順、DESCは降順に並び替える